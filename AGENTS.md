# Project instructions

Before starting any task, read `PROJECT_SPEC.md` completely and follow it as the
main project specification.

`PROJECT_CONTEXT.md` contains supporting history. Consult it when the current
task needs additional context, but treat the real files and `PROJECT_SPEC.md` as
the sources of truth.

# Expo has changed

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any application code.
